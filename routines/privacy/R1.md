---
routine_id: R1
version: 1.0.0
track: privacy
trigger_spec: "cron 0 18 * * * (daily 18:00, firm timezone)"
allowed_skills: [compensation-attribution, my-curator, reporting-business-review]
mcp_servers: [github-mcp, desktop-commander, 4thtech, excel-mcp]
recommended_model: qwen-3-14b-instruct (or larger if available)
last_updated: 2026-05-08
---

# R1 — Daily Output Capture (privacy)

**Trigger:** OS-native scheduling on the always-on machine, daily 18:00 (firm timezone).

## launchd plist (macOS)

```xml
<!-- ~/Library/LaunchAgents/oot.r1.plist -->
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key><string>oot.r1.daily-output-capture</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/bin/llmster</string>
        <string>--model</string><string>qwen-3-14b-instruct</string>
        <string>--skill</string><string>compensation-attribution</string>
        <string>--skill</string><string>my-curator</string>
        <string>--prompt-file</string>
        <string>/Users/oot/oot-framework/routines/privacy/r1.prompt.md</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict><key>Hour</key><integer>18</integer><key>Minute</key><integer>0</integer></dict>
    <key>StandardOutPath</key><string>/Users/oot/oot-framework/logs/r1.log</string>
    <key>StandardErrorPath</key><string>/Users/oot/oot-framework/logs/r1.err</string>
</dict>
</plist>
```

Install: `launchctl load ~/Library/LaunchAgents/oot.r1.plist`. Verify: `launchctl list | grep oot.r1`.

## cron (Linux)

```cron
# R1 daily output capture — 18:00
0 18 * * * /usr/local/bin/llmster --model qwen-3-14b-instruct --skill compensation-attribution --skill my-curator --prompt-file ~/oot-framework/routines/privacy/r1.prompt.md >> ~/oot-framework/logs/r1.log 2>&1
```

Install: `crontab -e`, paste, save. Verify: `crontab -l`.

## Task Scheduler (Windows)

XML template at `examples/task-scheduler/r1.xml`. Install: `schtasks /create /xml "r1.xml" /tn "ØØT R1 Daily Output Capture"`.

## Prompt body

Identical to the cloud version (`routines/cloud/R1.md`), with these source substitutions:
- GitHub: via GitHub MCP (PAT from secrets store).
- Slack channels → 4thtech dChat threads (`#output`, `#commercial`, `#sales`).
- Google Workspace read → local filesystem via Desktop Commander MCP, scanning `{{TRACKED_FOLDERS}}` paths.
- Slack post target → 4thtech dChat `#output-log`.
- Excel writeback: same openpyxl operation against a local Brain-repo clone — no Excel MCP needed for the Routine path. (Excel MCP remains optional for ad-hoc human-in-the-loop work.)

The full prompt body is identical otherwise (the deterministic rework-detection rule, the X1 cross-workbook discipline, the Brain daily-log discipline). Per ADR-001, both cloud and privacy tracks perform the same openpyxl + signed-commit operation; only the substrate differs.

## Expected outputs

- Rows appended to `firm/excel/partner-output-ledger.xlsx` in the local Brain-repo clone, signed-committed and pushed to `main`.
- Brain page at `firm/output-logs/YYYY-MM-DD.md`.
- 4thtech dChat post to `#output-log`.

## Setup checklist (privacy track)

1. Verify the always-on machine is configured (Mac mini / NUC / Pi 5 with FDE + UPS) per Skill Pack S12 §4.2.
2. Install `llmster` (`brew install llmster` on macOS or per LM Studio docs).
3. Confirm LM Studio MCP host is running with the required servers (github-mcp, desktop-commander, 4thtech). Excel MCP is **optional** — the Routine itself uses openpyxl directly via `llmster` code execution.
4. Place the prompt file at `~/oot-framework/routines/privacy/r1.prompt.md` (separate file containing the prompt body).
5. Create `~/oot-framework/logs/` directory.
6. Install the launchd plist (macOS), cron entry (Linux), or Task Scheduler XML (Windows) per platform.
7. Manual test fire: `launchctl start oot.r1.daily-output-capture` (or equivalent). Verify X1 row + Brain page + dChat post.

## Cloud-track equivalent

See `routines/cloud/R1.md`.
