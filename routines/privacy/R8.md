---
routine_id: R8
version: 1.0.0
track: privacy
trigger_spec: "cron 0 8 * * 1 (Monday 08:00)"
allowed_skills: [finance-treasury]
mcp_servers: [excel-mcp, 4thtech]
recommended_model: qwen-3-14b-instruct
last_updated: 2026-05-08
---

# R8 — Treasury Runway Update (privacy, OPTIONAL)

**Trigger:** OS-native scheduling, Monday 08:00.

> ⚠️ **OPTIONAL Routine.** Required only for organisations adopting the Unit Fund (Generation 2).

## cron (Linux)

```cron
0 8 * * 1 /usr/local/bin/llmster --skill finance-treasury --prompt-file ~/oot-framework/routines/privacy/r8.prompt.md >> ~/oot-framework/logs/r8.log 2>&1
```

## launchd plist (macOS)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key><string>oot.r8.treasury-runway</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/bin/llmster</string>
        <string>--skill</string><string>finance-treasury</string>
        <string>--prompt-file</string>
        <string>/Users/oot/oot-framework/routines/privacy/r8.prompt.md</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict><key>Weekday</key><integer>1</integer><key>Hour</key><integer>8</integer><key>Minute</key><integer>0</integer></dict>
</dict>
</plist>
```

## Prompt body

Identical to `routines/cloud/R8.md` with these substitutions:
- Email → 4thtech dMail.
- Slack `#treasury` → 4thtech dChat `#treasury`.
- Excel writeback: same openpyxl operation against a local Brain-repo clone (per ADR-001; no Excel MCP needed for the Routine path).

## Setup checklist (privacy)

1. Confirm X8 exists. If not adopting Unit Fund, skip R8.
2. Configure banking API access (jurisdiction-specific; most banks expose HTTP APIs that work identically on cloud + privacy track).
3. Place prompt at `~/oot-framework/routines/privacy/r8.prompt.md`.
4. Install launchd plist or cron entry.
5. Manual test fire.

## Cloud-track equivalent

See `routines/cloud/R8.md`.
