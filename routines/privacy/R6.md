---
routine_id: R6
version: 1.1.1
track: privacy
trigger_spec: "cron 0 23 * * * (daily 23:00)"
allowed_skills: [governance-compliance]
mcp_servers: [github-mcp]  # NOT my-curator — R6 reads other Routines' decisions from their Ledger writebacks, not the Curator graph (matches cloud R6 v1.1.1)
ledger_repo_access: required  # signed-commit + push of the audit log
code_execution: required     # openpyxl writes X7 Audit_Log_Index (ADR-001)
recommended_model: qwen-3-14b-instruct
last_updated: 2026-07-03
---

# R6 — EU AI Act Audit Trail (privacy)

**Trigger:** OS-native scheduling, daily 23:00.

> ⚠️ Signing key on the always-on machine is stored in a Trezor or YubiKey-backed slot, never plaintext. `gpg-agent` configured to cache passphrase for 24h.

## cron (Linux)

```cron
0 23 * * * cd ~/{{FIRM_SLUG}} && /usr/local/bin/opencode run --model lmstudio/qwen-3-14b-instruct "$(cat ~/oot-framework/routines/privacy/r6.prompt.md)" >> ~/oot-framework/logs/r6.log 2>&1
```

> Harness = OpenCode (`opencode run`); model server = local LM Studio hosted by the `llmster` daemon; the `cd ~/{{FIRM_SLUG}}` runner directory carries the scoped `opencode.json` allow-list (the unattended exception — its `git commit -S` pattern must be on the allow-list for the signed audit commit to land without a prompt). See R1's note and [`OPENCODE-SETUP.md`](../../installer/agent-assisted/OPENCODE-SETUP.md) → "Scheduled / unattended runs".

## launchd plist (macOS)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key><string>oot.r6.audit-trail</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-lc</string>
        <string>cd ~/{{FIRM_SLUG}} && /usr/local/bin/opencode run --model lmstudio/qwen-3-14b-instruct "$(cat ~/oot-framework/routines/privacy/r6.prompt.md)"</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict><key>Hour</key><integer>23</integer><key>Minute</key><integer>0</integer></dict>
</dict>
</plist>
```

## Prompt body

Identical to `routines/cloud/R6.md`. The signed-commit discipline is identical; the signing key is on the always-on machine's Trezor/YubiKey-protected slot.

## Setup checklist (privacy)

1. **Configure branch protection** (one-time): same as cloud. Force-push disabled, signed commits required, ≥1 reviewer for `firm/audit-logs/*`.
2. **Configure GPG signing on the always-on machine.** Generate or import the `oot-bot` GPG key. Configure `gpg-agent` for 24h passphrase cache. Test: `git commit -S -m test`.
3. **Trezor/YubiKey-backed signing slot** strongly recommended for production privacy-track instances.
4. Place prompt at `~/oot-framework/routines/privacy/r6.prompt.md`.
5. Install launchd plist or cron entry.
6. Manual test fire; verify a signed commit lands on the protected branch.

## Cloud-track equivalent

See `routines/cloud/R6.md`.
