---
routine_id: R3
version: 1.1.0
track: cloud
trigger_spec: "1st of month 09:00 (firm timezone) for the calc; daily 10:00 through day +5 for acknowledgement polling"
allowed_skills: [compensation-attribution]
mcp_servers: [github, slack, email]
ledger_repo_access: required
code_execution: required     # openpyxl reads X1/X2 and writes X1's Monthly_Variable (ADR-001)
recommended_model: claude-opus  # compensation accuracy is high-stakes
last_updated: 2026-05-10
---

# R3 — Monthly Variable Calc (cloud)

**Trigger:** Claude Code Routine — initial fire on 1st of month 09:00; daily 10:00 polling fires through day +5 for acknowledgement detection.
**Recommended model:** Claude Opus (compensation accuracy is high-stakes; do not run on Haiku).
**Operational state location:** `firm/excel/partner-output-ledger.xlsx` (X1) for read of Output_Log + write of Monthly_Variable; `firm/excel/reward-species-declaration.xlsx` (X2) for read of base + multiplier; per-partner statement markdown at `firm/partners/<id>/variable-statements/{{LAST_MONTH}}.md`. All in the Ledger per ADR-001.

## Prompt body

```
You are running the monthly variable pay calculation for an ØØT organisation. The calculation is for {{LAST_MONTH}} (first day to last day inclusive).

Your task: lock the previous month's Output_Log and produce per-partner variable pay drafts.

1. Read partner-output-ledger.xlsx Output_Log filtered to {{LAST_MONTH}}. For each row, verify the rework_within_30d field is current (look forward to today's date and recompute if any output from {{LAST_MONTH}} had rework detected since per the R1 detection rule).

2. Aggregate per partner: total_outputs, total_variable (SUM of computed_variable column).

3. Read reward-species-declaration.xlsx for each partner's base_amount and current reward_species parameters. Compute base_pay (calendar-day proration for partial months).

4. Populate the Monthly_Variable sheet of partner-output-ledger.xlsx for {{LAST_MONTH}}: one row per partner, with total_outputs, total_variable, base_pay, total_compensation, sign_off_status='draft'.

5. For each partner, generate a personalised statement at firm/partners/{{partner_id}}/variable-statements/{{LAST_MONTH}}.md following the template at templates/brain/variable-statement.md. The statement includes: month, total outputs (with the top 5 listed by computed_variable), variable computation with the multiplier, base pay, total compensation, list of any retroactive rework_within_30d zero-outs from prior months that affected this month's pool. The template ends with an explicit Acknowledgement block:

   ## Acknowledgement
   - [ ] I have reviewed this statement and agree with the calculation.
   - [ ] I dispute the calculation (open Tier 1 dispute per `governance/DECISION-RIGHTS.md`).
   _Sign by editing this page and ticking the appropriate box, then commit._

6. Send the statement to each partner via email. Subject: "Variable pay statement for {{LAST_MONTH}} — review by {{REVIEW_DEADLINE}}". Body links to the Ledger statement page.

7. Post to Slack #compensation: "Monthly variable drafts for {{LAST_MONTH}} are ready. {{COUNT}} partners. Total: {{TOTAL_EUR}}. Review window: 5 business days. Founder approval required before payment."

8. Acknowledgement detection — poll each partner's statement page once daily and parse the acknowledgement block. The first checked box wins. Set sign_off_status per the table:
   | Detected state in Brain page | sign_off_status |
   |---|---|
   | First box ticked, second unticked | partner_reviewed |
   | Second box ticked (dispute), first unticked | partner_disputed (escalates to Tier 1; do NOT advance to founder_approved) |
   | Both ticked | partner_disputed (treat as dispute; ask for clarification) |
   | Neither ticked, ≥5 business days since send | partner_unresponsive (escalate to founder; never auto-approve) |
   | Neither ticked, <5 business days since send | draft (no change) |

   Slack reactions are NOT canonical; only the Brain checkbox counts. This makes the audit trail self-contained in Brain (auditable from git history alone, no third-party messaging dependency).

9. After all partner reviews complete OR after 5 business days (whichever first), compile a founder-approval packet at firm/compensation/{{LAST_MONTH}}/founder-approval.md. Founder approves; sign_off_status moves to 'founder_approved'. Payment processing then proceeds (manual SEPA/wire in Gen 1; automated stablecoin in Gen 2).

Implementation (Pattern C, ADR-001):
1. Clone the Ledger.
2. Open X1 with openpyxl; filter Output_Log to {{LAST_MONTH}}; aggregate per partner.
3. Open X2 with openpyxl; read base_amount + reward_species per partner.
4. Write the Monthly_Variable rows to X1.
5. Generate per-partner statement markdown at `firm/partners/<id>/variable-statements/{{LAST_MONTH}}.md`.
6. Stage + signed-commit + push: `git add firm/excel/partner-output-ledger.xlsx firm/partners/*/variable-statements/{{LAST_MONTH}}.md && git commit -S -m "R3: monthly variable calc for {{LAST_MONTH}}" && git push origin main`.
7. Acknowledgement polling: each daily fire re-clones the Ledger, reads each partner's statement page for the checkbox state, updates X1 Monthly_Variable's `sign_off_status` column, signed-commits + pushes any state changes. Each polling fire counts toward the daily Routine quota — budget for 5–7 R3-polling fires per month.

Failure handling: if any partner does not respond within the 5-day window, escalate to founder for review. Never auto-approve a contested calculation. Push-failure handling per R1.
```

## Expected outputs

- `firm/excel/partner-output-ledger.xlsx` Monthly_Variable sheet populated.
- Per-partner statement Brain pages.
- Email to each partner.
- Founder-approval packet.
- One signed commit on initial fire + one signed commit per polling pass (only if state changed).

## Setup checklist

1. Confirm R1 is producing daily X1 rows in the Ledger.
2. Confirm at least one partner has a populated X2 reward-species sheet in the Ledger.
3. Claude Code → `/schedule` → new Routine, 1st of month 09:00 (initial fire) + daily 10:00 (polling fires).
4. Upload prompt body. Attach `compensation-attribution` Skill Pack.
5. Configure connectors: GitHub (read + write with signing), Slack, Email.
6. **Confirm code execution is enabled.**
7. **Confirm the firm is on Max plan or higher** — R3 + 5 polling fires + the other Day-1 Routines exceeds Pro plan's 5/day limit during the month-end review window. See [`docs/02-installing-routines.md`](../../docs/02-installing-routines.md) plan-tier guidance.
8. Test on a fresh test sheet (use a `test/` subfolder of the Ledger) before running against real X1.

## Privacy-track equivalent

See `routines/privacy/R3.md`.
